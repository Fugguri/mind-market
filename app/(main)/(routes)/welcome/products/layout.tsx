export const metadata = {
  title: "Продукты",
  description: "Наши технологические решения",
};

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
