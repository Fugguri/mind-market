/** @type {import('next').NextConfig} */
const nextConfig = {
    
    images: {
        domains:[
            "uploadthing.com",
            "utfs.io",
            "localhost:8000",
            'avatars.githubusercontent.com',
        ]
    }
}

module.exports = nextConfig
